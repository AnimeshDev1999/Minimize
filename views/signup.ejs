<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Meta and links -->
    <%- include('./partials/headData') %>
    <title>Minimize | Signup</title>
  </head>
  <body class="flex flex-col justify-between">
    <!-- Navbar -->
    <%- include('./partials/navBar') %>
    <!-- Page content -->
    <div
      class="grow lg:mx-auto m-5 lg:m-10 max-w-[1200px] p-5 lg:p-10 rounded-md bg-cyan-700/20"
    >
      <h2 class="text-2xl mb-2">Welcome guest</h2>
      <p class="mb-8">Let's get you an account to get started</p>
      <form class="flex flex-col" method="post" action="/api/users/create">
        <label class="">Name</label>
        <input
          class="bg-slate-600/50 rounded-md mt-2 mb-4 px-2 py-2"
          name="name"
          type="Text"
          required
        />
        <label class="">Email</label>
        <input
          class="bg-slate-600/50 rounded-md mt-2 mb-4 px-2 py-2"
          name="email"
          type="email"
          required
        />
        <label class="">Password</label>
        <input
          class="bg-slate-600/50 rounded-md mt-2 mb-10 px-2 py-2"
          name="password"
          type="password"
          required
        />
        <button type="submit" class="btn-anim mb-4 px-4 py-2">Signup</button>
      </form>
      <p class="mt-5 text-center">
        Already have a account?
        <a class="underline text-sky-600" href="/login">LogIn</a> instead.
      </p>
    </div>
    <!-- Footer -->
    <%- include('./partials/footer') %>
  </body>
</html>
